<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GroceryOrderingApp.Mobile.Views.CartPage"
             Title="Cart">
    <VerticalStackLayout Padding="10">
        <ActivityIndicator IsRunning="{Binding IsLoading}" IsVisible="{Binding IsLoading}"/>
        
        <Label Text="{Binding CartItems.Count, StringFormat='Items in Cart: {0}'}" FontSize="14" FontAttributes="Bold"/>
        
        <CollectionView ItemsSource="{Binding CartItems}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Padding="15" Margin="5" HasShadow="True" CornerRadius="8">
                        <StackLayout>
                            <Label Text="{Binding ProductName}" FontSize="16" FontAttributes="Bold"/>
                            <Label Text="{Binding Price, StringFormat='₹{0:F2}'}" FontSize="12" TextColor="Gray"/>
                            <HorizontalStackLayout>
                                <Label Text="Qty: " FontSize="12"/>
                                <Label Text="{Binding Quantity}" FontSize="12"/>
                                <Button Text="Update" Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.UpdateQuantityCommand}" CommandParameter="{Binding .}" FontSize="10" Padding="5"/>
                                <Button Text="Remove" Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.RemoveItemCommand}" CommandParameter="{Binding .}" FontSize="10" Padding="5" TextColor="Red"/>
                            </HorizontalStackLayout>
                            <Label Text="{Binding TotalPrice, StringFormat='Total: ₹{0:F2}'}" FontSize="12" FontAttributes="Bold"/>
                        </StackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        
        <Label Text="{Binding TotalAmount, StringFormat='Cart Total: ₹{0:F2}'}" FontSize="16" FontAttributes="Bold" Margin="0,20,0,0"/>
        
        <Button Text="Place Order" Command="{Binding PlaceOrderCommand}" Margin="0,10,0,0" Padding="10" IsEnabled="{Binding CartItems.Count, Converter={StaticResource CountToBool}}"/>
    </VerticalStackLayout>
</ContentPage>
